import React from 'react'

export const Chat = () => {
  return (
    <div>
        <h2>Chat</h2>
        <p>Left side contain chats conversation with user and right contains chatbox where message are recived and send </p>
        <strong>Similar to  whatsApp</strong>
        <strong>group chat: user send request to user , and admin will accept then only added to group chat</strong>
        {/* <strong></strong> */}
    </div>
  )
}
